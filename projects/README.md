# Projects

Projects turn a commercial objective into a bounded multi-agent workstream.

Use `P###_short_name/` and start from `templates/PROJECT_CHARTER_TEMPLATE.md`. Atlas CEO owns project creation, status and closure. Agent evidence remains in `intelligence/`; canonical records remain in `database/`.

